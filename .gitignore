.DS_Store
.vercel/
node_modules/
*.log
.vercel
.env*.local

# 部署備份垃圾桶，不進版控
_trash/

# 本機校稿器（tuner）：只在本機開來拉數值用，不進版控也不上線
# 2026-08-07 加，因為 preflight 第 4 關會要求所有 HTML 都進 sitemap，
# 這類工具檔本來就不該有公開網址。
_tuner-*.html
